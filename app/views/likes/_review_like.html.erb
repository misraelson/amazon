
<% if review.likes.where(user_id: current_user.id).present? %>
  <%- @user_like = review.likes.where(user_id: current_user.id).first %>
  <%= link_to "Un-Like", product_review_like_path((@product || product), review,  @user_like), method: :delete, remote: true %>
<% else %>
  <%= link_to "like", product_review_likes_path((@product || product), review), method: :post, remote: true %>
<% end %>
(<%= @review.likes.count %>)
